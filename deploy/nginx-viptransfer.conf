# Eski domain -> yeni domain 301 kalıcı yönlendirme (SEO korunur)
server {
    listen 80;
    server_name viptransferglobal.com www.viptransferglobal.com;
    return 301 https://viptransfer.com$request_uri;
}

# Ana domain
server {
    listen 80;
    server_name viptransfer.com www.viptransfer.com;

    location / {
        proxy_pass http://127.0.0.1:3001;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;
    }
}
